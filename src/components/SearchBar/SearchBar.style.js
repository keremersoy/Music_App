import { StyleSheet } from "react-native"

export default StyleSheet.create({
    container:{
        backgroundColor:"#eceff1",
        paddingHorizontal:8,
        marginHorizontal:4,
        marginVertical:2,
        borderRadius:10,
    }
});
